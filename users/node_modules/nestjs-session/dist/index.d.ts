import { AsyncOptions, SyncOptions } from 'create-nestjs-middleware-module';
import * as expressSession from 'express-session';
import { createRetriesMiddleware } from './retriesMiddleware';
interface Options {
    session: expressSession.SessionOptions;
    retries?: number;
    retriesStrategy?: Parameters<typeof createRetriesMiddleware>[2];
}
export declare type NestSessionOptions = SyncOptions<Options>;
export declare type NestSessionAsyncOptions = AsyncOptions<Options>;
export declare const SessionModule: import("create-nestjs-middleware-module").FacadeModuleStatic<Options>;
export {};
